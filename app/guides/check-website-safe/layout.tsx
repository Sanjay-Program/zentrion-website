import { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'How to Check if a Website is Safe Before You Click (2026)',
  description: 'Check if a website is safe using SSL verification, security headers, URLhaus, and threat feed lookups. CLI commands and free tools included.',
  keywords: 'check website safe, is website safe, url safety check, website security check, ssl checker',
};
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
