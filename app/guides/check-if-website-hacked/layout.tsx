import { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'How to Check if Your Website Has Been Hacked (2026)',
  description: 'Step-by-step guide to detect if your website is hacked. Find malicious redirects, web shells, suspicious outbound connections, and injected malware using CLI commands.',
  keywords: 'website hacked check, how to know if website is hacked, web shell detection, malicious redirect check',
};
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
